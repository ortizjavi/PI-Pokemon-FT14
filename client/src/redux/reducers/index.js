// import { GET_MOVIES, GET_MOVIE, ADD_FAVORITE } from '../constants'


const initialState = {
};


const reducer = (state = initialState, action) => {
	switch (action.type){
		default:
			return state;
	}
}

export default reducer;